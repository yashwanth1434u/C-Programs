#include<stdio.h>
#include<string.h>
int main()
{
	char str[100];
	int i;
	printf("enter a string:");
	gets(str);
	for(i=0;str[i]!='\0';i++)
	{
	  if(str[i]=='a')
	  	str[i]=str[i]-32;
	}
	puts(str);
	return 0;
}
