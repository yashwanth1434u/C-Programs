//HARSHAD OR NOT
#include<stdio.h>
int main()
{
    int num,n,sum=0,r;
    scanf("%d",&num);
    n=num;
    while(n>0)
    {
        r=n%10;
        sum=sum+r;
        n=n/10;
    }
    if(n%sum==0)
    printf("%d is Harshad Number",num);
    else
    printf("%d is not Harshad Number",num);
    
}
